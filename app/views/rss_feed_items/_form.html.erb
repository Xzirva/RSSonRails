<%= form_for(@rss_feed_item) do |f| %>
  <% if @rss_feed_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rss_feed_item.errors.count, "error") %> prohibited this rss_feed_item from being saved:</h2>

      <ul>
      <% @rss_feed_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
